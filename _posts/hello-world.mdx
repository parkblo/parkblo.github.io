---
title: "Hello World: MDX 종합 가이드"
date: "2024-01-19"
description: "parkblo 블로그의 모든 마크다운 및 MDX 기능을 테스트하고 시연하기 위한 종합 가이드입니다."
category: "Development"
icon: "pixelart-icons-font-ship"
---

# 안녕, 세상아! 🚀

MDX 테스트 포스트에 오신 것을 환영합니다. 이 포스트는 **parkblo** 블로그의 레이아웃과 콘텐츠 레이어에서 지원하는 모든 기능을 시연하기 위해 작성되었습니다.

## 1. 타이포그래피 & 기본 마크다운

**굵은 텍스트**, _이탤릭 텍스트_, 또는 ~~취소선~~을 사용할 수 있습니다.

### 리스트 (Lists)

- 순서 없는 리스트 아이템 1
- 순서 없는 리스트 아이템 2
  - 중첩된 아이템
- 순서 없는 리스트 아이템 3

1. 순서 있는 리스트 아이템 1
2. 순서 있는 리스트 아이템 2
3. 순서 있는 리스트 아이템 3

### 할 일 목록 (Task Lists)

- [x] 레이아웃 중앙 정렬
- [x] MDX 통합
- [x] 리액션 상태 유지 (Persistence)
- [ ] SEO 최종 점검

---

## 2. 고급 블록 (Advanced Blocks)

> [!NOTE]
> 이것은 GitHub 스타일의 **Note** 콜아웃입니다. 중요한 문맥을 강조할 때 유용합니다.

> [!TIP]
> 표준 블록인용(blockquote)도 사용할 수 있지만, 이런 커스텀 콜아웃이 훨씬 보기 좋습니다!

### 커스텀 컴포넌트

<PixelBox title="비밀 메모">
  MDX를 통해 렌더링된 커스텀 `PixelBox` 컴포넌트입니다! 픽셀 스타일의 테두리와
  절대 위치로 배치된 제목이 특징입니다.
</PixelBox>

### 문법 강조가 적용된 코드 블록

```typescript
// src/lib/mdx.ts
import fs from "fs";
import path from "path";
import matter from "gray-matter";

export function getPostBySlug(slug: string) {
  const fullPath = path.join(process.cwd(), "_posts", `${slug}.mdx`);
  const fileContents = fs.readFileSync(fullPath, "utf8");
  const { data, content } = matter(fileContents);
  return { slug, ...data, content };
}
```

### 인라인 코드

문단 중간에 `inline code`를 삽입할 수도 있습니다.

---

## 3. 수학 공식 (KaTeX)

$a \ne 0$일 때, $ax^2 + bx + c = 0$의 해는 다음과 같습니다:

$$
x = \frac{-b \pm \sqrt{b^2 - 4ac}}{2a}
$$

사건 $B$가 주어졌을 때 사건 $A$의 확률 $P(A|B)$:

$$
P(A|B) = \frac{P(B|A)P(A)}{P(B)}
$$

---

## 4. 미디어 & 링크

### 링크

제 [GitHub 프로필](https://github.com)을 확인하거나 [Next.js](https://nextjs.org)에 대해 더 알아보세요.

### 이미지 (Mockup)

![픽셀 아트 목업](https://images.unsplash.com/photo-1550745165-9bc0b252726f?auto=format&fit=crop&q=80&w=800)
_픽셀 스타일의 이미지 캡션 예시입니다._

---

## 5. 테이블 (Tables)

| 기능 (Feature) | 상태 (Status) | 우선순위 (Priority) |
| :------------- | :-----------: | :------------------ |
| 레이아웃       |      ✅       | 높음                |
| MDX            |      ✅       | 높음                |
| 리액션         |      🛠️       | 중간                |
| SEO            |      ⏳       | 낮음                |

---

## 결론

이 포스트를 통해 우리의 MDX 렌더링 엔진이 모든 카테고리에서 완벽하게 작동함을 확인했습니다!
